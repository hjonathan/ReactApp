import * as ImageControl from "./imagePicker";
import * as FileControl from "./filePicker";

const MobileControls = Object.assign({}, {
    ImageControl,
    FileControl
});
export default MobileControls;
