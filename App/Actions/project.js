const actions = {
    /**
     * Actions to manage the projects, tasks and steps
     */
    project: {
        task: {
            steps: {
                request: x => x,
                success: x => x,
                error: x => x
            }
        }
    }
};

export default actions;