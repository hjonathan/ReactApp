import {StyleSheet} from "react-native";

export default StyleSheet.create({
    searchBox: {
        color: "#eee",
        marginHorizontal: 4,
        paddingHorizontal: 8,
        height: 40,
        width: 200
    }
});
