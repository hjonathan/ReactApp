import {StyleSheet} from "react-native";

export default StyleSheet.create({
    button: {
        alignItems: "center",
        borderRadius: 50,
        borderWidth: 2,
        borderColor: "#FFFFFF",
        paddingHorizontal: 50,
        paddingVertical: 10
    },
    text: {
        color: "#FFFFFF",
        fontWeight: "bold"
    }
});
